services:
  - type: web
    name: infinity-admin
    runtime: node
    plan: free
    autoDeploy: true
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        value: InfinityX9!
      - key: PUBLIC_BASE_URL
        value: https://www.infinityhub.space
